def square_array(numbers)
  squared_array = []
  numbers.each do |num|
  square = num * num
  squared_array << square
  end
squared_array
end

def square_array(numbers)
  squared_array = []
  numbers.map {|num| square = num * num}
  squared_array << square
  squared_array(numbers)
end
